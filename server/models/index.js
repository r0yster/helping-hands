const User = require('./User');
const Post = require ('./Post');
const Comment = require ('./Comment');
const Volunteer = require('./Volunteer');

module.exports = { User, Post, Comment, Volunteer };
